Confirmed the contradiction-can't-block-ship finding: on the keyless no-host path, NEUTRAL-band findings get `score=0.5` (PARTIAL), which is ≥ PARTIAL_LOW, so they map to severity "major" — never "critical" — and `gate_blocks_ship` only blocks on critical. A flatly-contradicting cite the keyless lane can't distinguish from NEUTRAL becomes a non-blocking MAJOR. The comment even admits "the gate doesn't ship-block." Quality-evidence's finding holds.

I now have all evidence verified independently. Synthesizing the final verdict.

---

# Bad Research — Re-Audit Verdict (post-remediation, PR #7 / merge 7203039)

## 1. New verdict

**Mixed, leaning genuinely good — real engineering with real loose ends, not theater. New real-value score: 7/10. Delta vs prior: IMPROVED (+1).** The remediation is substantive and verifiable, not cosmetic: both prior CRITICALs are fixed with real, regression-guarded code (grader is in the install roster with a teeth-bearing diff test I mutation-tested; the keyless number/negation rubber-stamp is closed deterministically; both keyless CLIs no longer crash on the in-pipeline path), and every honesty-surface defect the prior audit named is materially cleaner (golden eval self-labels as a regression-smoke-gate with a disclaimer and disclosed skips; the unsourced 55.9/52.6 is gone from all user-facing text; the head-to-head is honest scaffolding that says outright it proves nothing yet). The core pipeline value over Claude+WebSearch (three-tier backward grounding, patch-never-regenerate tool-locks, separate-corpus adversarial critics, deterministic uncited ship-gate) survived intact. It is capped at 7 because the remediation was verified **narrowly** — against the one test each fix touched, not end-to-end — leaving a cluster of confirmed loose ends: a second integrity gate that still omits the 5th critic, two CRITICAL fixes that don't compose on a keyless full run, a standalone keyless crash in `verify-citations`, a same-number antonym contradiction still rubber-stamped, an over-claim in a linked doc, and three newly-introduced defects (a broken README CLI example, invalid Codex SKILL.md YAML, and Codex render leaks). The flagship "beats commercial DR" claim remains unproven (zero real competitor runs).

## 2. Did the fixes land?

| Prior fix | Status | Evidence (verified independently) |
|---|---|---|
| **CRITICAL #1**: grader in roster + guard test | **landed-correct** | `hooks.py:3749` (21-entry roster); `test_all_skills_valid.py:62-76` diffs every `Skill()` target vs roster; I mutation-tested it (dropped the grader line → test FAILS with the exact missing-target message). 5 tests pass. Loop is reachable (`bad-research.md:126`). |
| **CRITICAL #2a**: grade-report keyless degrade | **landed-correct** | `research.py:557-572` try/except → `{status:keyless-skip,passed:null}`, exit 0. Reproduced live. |
| **CRITICAL #2b**: verify-citations keyless degrade | **landed-incomplete** | The keyless *provider* degrade (`research.py:393-399`) + worklist (`verifier.py:418-428`) are correct. BUT `_verify_report` (`research.py:368`) calls `Vault.discover()` with no try/except and never calls `store.init_schema()` — unlike sibling `_uncited_gate` (`:491-501`). **Reproduced both crashes live standalone keyless**: `VaultError` (no vault), then `OperationalError: no such table: claim_anchors` (vault but no schema), *before* the degrade runs. In-pipeline fine; standalone keyless broken. |
| **CRITICAL row6**: numeric/negation guard | **landed-correct (with a known hole)** | `gate.py:273-281` wired into both judges (`verifier.py:79-86,:125-130`). Live: number-flip `12%`/`21%` and `passed`/`failed` escalated. Conservative (only adds). See §4 for the antonym hole the fix does NOT cover. |
| **MAJOR**: golden eval self-labels smoke-gate | **landed-correct** | `golden.py:266-288` + on-disk report: `gate_type:regression-smoke-gate`, full disclaimer, `skipped:2`, `total:8`, `pass_rate:1.0`. Honest at every layer. |
| **MAJOR**: remove 55.9/52.6 from entry skill | **landed-correct (user-facing)** | Zero hits in `bad-research.md`, README, HOW_IT_WORKS. Survives only in archived research/investigation notes (one now-dangling file:line pointer in `round1-current-state.md:360`). |
| **MAJOR row5**: single-source critic count | **landed-incomplete (present-but-wrong elsewhere)** | Entry skill fixed (5 critics, gate includes assumption). But **`bad-research-15-polish.md:86-99` integrity gate still loops only 4 critics, omits `critic-findings-assumption.json` AND `grader-log.json`**, prose says "all four critic findings." Plus ~9 stale "four/4 critic" sites incl. the **shipped** light-critic agent (`hooks.py:1294`: "four separate Opus critics"). Guard test only reads entry + step-12 skills. |
| **MAJOR row6**: README route levers | **landed-but-wrong (new defect)** | README:59-71 documents `--ultrafast`/`--effort` (genuine). BUT README:75 `bad route <your question> --json` **fails live with exit 2 "Missing option --decomposition"**; route_cmd docstring still says "(fast\|full)". Introduced by PR #7. |
| **MINOR row13**: stale Layer/comparisons prose | **landed-incomplete** | Width-sweep legacy demotion clean; `comparisons.md`→`tensions.md` done. But `hooks.py:6,:3145-3150` docstrings still say "four critics"/"Layer 1-7"; HOW_IT_WORKS.md untouched (see §4). |
| **CODEX rows14-15**: installer + unverified primitives | **landed-incomplete (new defects)** | Installer real, single-sourced, idempotent; colon-form dispatch fixed; primitives honestly marked ASSUMED. BUT see §4: invalid SKILL.md YAML, render leaks, dangling paths. |

## 3. What's genuinely good now (verified, ranked)

1. **The flagship CRITICAL is genuinely fixed and regression-guarded.** Grader in the 21-entry roster; the guard test diffs `Skill()` targets vs roster and I confirmed it has teeth via mutation. This is exactly the fix + test the prior audit demanded.
2. **The keyless number/negation rubber-stamp is closed deterministically and $0.** `gate.py:273-281`; verified live that number-flips and negation-token flips are escalated while aligned numbers still pass; the guard only ever *adds* an escalation (`verifier.py` control flow → NEUTRAL → pending), so it cannot mask a real support.
3. **Core pipeline value over Claude+WebSearch survived intact.** Three-tier backward grounding with a `needs_host_judgment` degrade (not a rubber-stamp), patch-never-regenerate tool-locks, separate-corpus critics, and a deterministic uncited ship-gate (`gate.py:182-185`) — none replicable by a one-shot prompt.
4. **Honesty surface is materially cleaner and now test-enforced.** Golden eval self-labels as a smoke-gate with a disclaimer + disclosed skips; the head-to-head module says "never proves a win" (`headtohead.py:6`) and HEAD_TO_HEAD.md says "no run has been executed... no claimed win on any axis"; no scorecard/manifest files exist (no faked numbers). The Codex spike honestly marks each primitive VERIFIED vs ASSUMED.
5. **1093 tests pass** (1 failure, 9 skipped). The single failure (`test_ladder`) is pre-existing (last touched at 3273f8a, an ancestor of PR7) and not a remediation regression.

## 4. What's still weak / bullshit / newly broken (ranked by severity)

1. **[CODEX, newly-introduced] Codex entry SKILL.md frontmatter is INVALID YAML — a potential hard load-blocker.** `to_codex_skill_frontmatter` flattens the folded `description: >` into an unquoted inline scalar; `yaml.safe_load` **fails at column 276** on the embedded `tier-adaptive:` colon (reproduced live). The guard test `test_skill_md_frontmatter_is_codex_valid` only `str.split(":")`-checks key names and never parses YAML, so it's falsely green. Generalizes to any skill whose description contains `": "`.
2. **[OPTIMIZE] The two CRITICAL fixes don't compose on a keyless FULL run.** `bad-research-12.5-grader.md` loop only branches on `passed==true`/`false` (`:93,:127`) — no null branch for the `passed:null` the keyless CLI now emits (verified: grep for null/keyless = nothing). Rounds 2-3 fall to the false-path patcher spawn with empty findings, wasting patcher rounds and writing a `grader-log.json` with `final_passed:False`/`overall:None` indistinguishable from a real quality failure. Bounded by cap=3 + ship-anyway, so it's wasted work + a misleading log, not a crash.
3. **[OPTIMIZE] Single-source-the-critic-count was a surface patch.** `bad-research-15-polish.md:86-99` integrity gate still checks only 4 critics, omits `critic-findings-assumption.json` + `grader-log.json`, prose says "all four" — diverging from the entry gate (`bad-research.md:332-343`) which includes both. A silently-missing assumption critic or grader passes this gate. Mitigation: the entry gate (post-step-16) would catch it later, so the run doesn't ship undetected — but the polish gate's self-heal stub never fires at the right stage.
4. **[OPTIMIZE] `bad verify-citations` still crashes standalone keyless.** Reproduced `VaultError` then `OperationalError: no such table: claim_anchors` (`research.py:368,:374`) *before* the keyless degrade runs — contradicting the "runs on a truly keyless box" framing. Sibling `_uncited_gate` shows the correct pattern (`:491-501`). Bounded: in-pipeline is fine.
5. **[OPTIMIZE] The number/negation guard still rubber-stamps same-number antonym contradictions keyless.** Verified live: `'GDP grew 2.1%'` vs `'GDP declined 2.1%'` → overlap 0.800, mismatch=False, **shortcut_accept=True**; same for `'ratified by all'` vs `'rejected by all'`. `_NEGATION` has no antonym table (grew/declined, rose/fell, ratified/rejected). The team's own `test_regression_a9.py:11-19` admits it reworded *away* from this exact pair. This is the more common faithfulness error than a bare number-flip.
6. **[OPTIMIZE] `needs_host_judgment` worklist has no consumer.** Grep: ZERO references in any skill or doc (only the verifier producer + one CLI comment). The 11.5/fast disposition tables key off score; a `needs_host_judgment` finding's fixed `score=0.5` maps to FLAG/hedge — so keyless paraphrases get *silently hedged, not re-judged*. The "usable worklist" half is inert.
7. **[OPTIMIZE] Keyless contradiction can never block ship.** A flatly-contradicting cite on the keyless no-host path gets `score=0.5` (≥ PARTIAL_LOW), → severity "major", and `gate_blocks_ship` only blocks "critical" (`gate.py:170-184`). The comment even says "the gate doesn't ship-block." The keyless lane structurally cannot reach the <0.40 CRITICAL band.
8. **[CODEX, newly-introduced] Codex leak-lint has a hole exactly where leaks occur.** Rendered SKILL.md still ships `PreToolUse` + `/bad-research` (lines 215, 352) and a **dangling mis-translated path** `references/bad-research-1-decompose/SKILL.md` (real file is `references/1-decompose.md`) plus a self-reinvoke pointing at `references/bad-research.md` (doesn't exist — entry is SKILL.md). `_FORBIDDEN` omits all of these. The `.claude/skills/`→`references/` literal sub bypasses `skillref_path` normalization.
9. **[BEAT-COMPETITORS] HOW_IT_WORKS.md still over-claims "no-hallucination / every report sentence is checked."** `docs/HOW_IT_WORKS.md:100-107`, untouched by PR7 — contradicting the keyless reality where local-NLI is a no-op and the host-gate only fires if a judge is wired (else `needs_host_judgment` trusts the orchestrator). README:90 was scoped; this linked doc was not.
10. **[MINOR] Residual drift:** ~9 stale "four/4 critic" sites incl. the shipped light-critic agent (`hooks.py:1294`); step count is 13-vs-19-vs-21 across prose/test/roster with no test pinning the "13"; route_cmd docstring still "(fast\|full)"; instruction-critic ships RAW on Claude (`hooks.py:3502`) but `.format()`ed on Codex — so the two installs are NOT byte-identical (the codex lens's "no drift possible" strength is overstated).

**Note on a lens disagreement I resolved:** pipeline-value called the `test_ladder` failure "fully-mocked, no Playwright, a genuine logic failure." That is **wrong** — I reproduced it: the injected `_tier0` mock is not honored and the call hits a live browser returning a real accessibility snapshot ("This site can't be reached"). It is an environment-coupled test-isolation failure, as operator-ux/quality-evidence framed it. Still pre-existing, not a regression.

## 5. Honest competitor positioning now

**Wider documentation, identical proof — the win is now MEASURABLE but still UNMEASURED.** What improved: the ultrafast/effort levers are documented (README:59-71) and CLI-wired, and a blinded, tie-never-rounded-to-win head-to-head apparatus with 17 honesty-pinning tests now exists. What did not: zero real competitor results ship anywhere (confirmed by `find` — no scorecard/manifest), so the only quantitative self-assessment remains the (now honestly-relabeled) regression-smoke gate at `pass_rate:1.0`. A genuine ceiling the competitor lens surfaced and I confirmed: **the keyless-by-default product cannot self-run a fair head-to-head** — the only content-quality judge (`--llm`) requires `ANTHROPIC_API_KEY` (reproduced: fails clean keyless), and the keyless `RubricJudge` auto-fails a corpus-less competitor (`source_quality:fail`) and will pass a report that contradicts its sources (overlap, not entailment). The moat is real and defensible only on the same narrow slice as before: keyless $0 marginal cost, persistent auditable vault, deterministic blocking uncited-claim ship-gate (no hosted DR exposes this), scholarly recall, route/effort controllability. It still loses on freshness, multimodal, raw speed, scale, and proof.

## 6. Remaining-work roadmap (high-impact-low-effort first)

| Work | Track | Impact | Effort |
|---|---|---|---|
| Fix Codex SKILL.md frontmatter to a quoted/folded scalar + make the guard test `yaml.safe_load` the block | [CODEX] | High | Low |
| Teach the 12.5-grader skill the `passed:null`/keyless-skip branch (grade inline or break) | [OPTIMIZE] | High | Low |
| Add `critic-findings-assumption.json` + `grader-log.json` to the polish gate; single-source the critic-findings filenames to one constant both gates + a test consume | [OPTIMIZE] | High | Low |
| Broaden the Codex leak-lint (`PreToolUse`, `/bad-research`, `--steps-only`, dangling `references/bad-research-*`); route bootstrap/self-reinvoke through `skillref_path` or drop on Codex | [CODEX] | High | Low |
| Make `_verify_report` standalone-safe (try/except VaultError + `init_schema()`) like its sibling | [OPTIMIZE] | Med | Low |
| Fix/remove the broken README `bad route <question>` example; fix route_cmd docstring to include ultrafast | [OPTIMIZE] | Med | Low |
| Scope HOW_IT_WORKS.md grounding section to keyless reality (match README:90) | [BEAT-COMPETITORS] | High | Low |
| Wire a `needs_host_judgment` consumer into the 11.5/fast/ultrafast disposition tables (host re-judges inline) | [OPTIMIZE] | High | Low |
| Add an antonym/directional-verb lexicon to the near-verbatim guard (rose/fell, ratified/rejected, grew/declined) | [OPTIMIZE] | High | Low |
| Feed host-judged contradictions back into verify_score so a keyless CONTRADICTED can reach the <0.40 CRITICAL ship-block band | [OPTIMIZE] | High | Med |
| Purge ~9 stale "four/4 critic" sites (esp. shipped light-critic agent); extend the count guard to all skills + shipped agent strings | [OPTIMIZE] | Med | Low |
| Actually RUN the head-to-head: 8-12 shared queries vs ONE commercial DR tool, `--llm`-judged + blinded, publish the scorecard | [BEAT-COMPETITORS] | High | High |

## 7. Bottom line

**Keep investing — do NOT ship-as-is, but nothing flagship-breaking is still broken.** The remediation genuinely worked on the load-bearing bugs: the project moved from 6/10 to 7/10 on real, tested, regression-guarded fixes, and the honesty surface is now clean and test-enforced. This is real engineering, not theater. But the fixes were verified one-test-at-a-time rather than end-to-end, which left a consistent pattern of loose ends: a duplicate gate still wrong, two fixes that don't compose keyless, a standalone keyless crash, an antonym contradiction still rubber-stamped, and three newly-introduced defects (broken README example, invalid Codex YAML, Codex render leaks). The single most urgent item before any Codex ship is the **invalid SKILL.md frontmatter** — it is a likely hard load-blocker the test suite is falsely green on. The full Claude pipeline route is sound in-pipeline; the rough edges are concentrated in standalone-keyless usage and the still-unverified Codex port. The flagship "beats commercial DR" claim is now honestly framed as undemonstrated — and remains so. Most of the remaining work is high-impact / low-effort; the one high-effort item (running a real head-to-head) is the only thing that converts the competitive pitch from defensible-on-a-narrow-slice to proven.