All load-bearing facts verified. Final confirmations:

- **`run_prefilter`/`passes_recency_gate` are defined ONLY in prefilter.py and called NOWHERE** — the entire recency apparatus (passive scoring AND active gating) is inert. This is even more dead than the competitive-gap lens claimed; the moat-skeptic's "already wired" assertion is FALSE.
- **`get_llm_provider` only offers anthropic** (raises otherwise); `LLMJudge.judge` is a clean single `provider.complete()` call producing JSON → `AxisRails.from_raw` → `from_rails`. The host-judge seam is a genuine drop-in at the `Judge` Protocol boundary — a `HostJudge` that writes the prompt to JSON and reads back rails is a real, small, keyless change.

I now have everything needed to synthesize the ranked road to 9.

---

# Bad Research — Road to 9/10 (Lead Architect Synthesis)

## 1. The gap

The verified 7 is a real engineering artifact with a narrow, genuine moat (keyless $0, persistent auditable vault, deterministic uncited-claim ship-gate, scholarly recall, controllability) — but both audits cap it at 7 for **two specific reasons that the code confirms**: (a) **proof is absent** — there are zero real competitor runs (`find` confirms no `runs/`/`results/` dir, only a toy 1-query example scorecard), and the keyless product *structurally cannot self-run a fair quality benchmark* because the only semantic judge (`headtohead --llm`) hard-requires `ANTHROPIC_API_KEY` (`get_llm_provider` offers only `anthropic`, which raises keyless) while the keyless `RubricJudge` is overlap-not-entailment; and (b) **two of the four "lost" competitive axes are not infra problems, they are dead wiring** — the entire recency apparatus (`published_days_ago`, `age_days`, `recency_days`, `run_prefilter`/`passes_recency_gate`, `RECENCY_MAX_AGE_DAYS`) is **never assigned and never called anywhere outside its own module** (verified: `Candidate` is built at `dedup.py:55` with no date; the prefilter recency gate has zero callers), and the host model is natively multimodal but **no skill ever tells it to read a figure** while the screenshot→assets persistence path and the documented `assets` CLI are vaporware (zero `INSERT INTO assets`; no `assets` subcommand). The distance from 7→9 is therefore: convert one undemonstrated flagship claim into a measured one, and activate/build the two capabilities the product claims-by-architecture but doesn't actually do. This is a **big jump** — it is not reachable by polish; it requires the proof artifact plus genuine new capability.

## 2. The road to 9 (ranked by verified Δ / effort)

| # | Lever | Attacks | Keyless? | Ships-now / needs-user | Effort | Honest Δ | Verdict |
|---|---|---|---|---|---|---|---|
| 1 | **HostJudge seam** — a `Judge`-Protocol impl that writes the blinded `JUDGE_SYSTEM`+report+corpus to JSON and reads host-written 5-rail verdicts back through `AxisRails.from_raw` (the existing drop-in seam), so the orchestrating Claude judges keyless | proof | yes | ships-now | med | **+0.4** | **Load-bearing enabler.** Without it the head-to-head can't be run honestly keyless. Real but an enabler, not the proof. |
| 2 | **Run + publish ONE real blinded head-to-head** (8–12 starter queries, ultrafast vs ONE commercial DR tool), host-judged via #1, commit scorecard to `runs/`/`docs/benchmarks/` | proof | yes (once #1 lands) | **needs-user** | high | **+0.5** | **The single biggest mover.** Both audits: "the only thing that converts the pitch from defensible-to-proven." Gated on #1 + user pasting competitor reports. |
| 3 | **Wire the host-model vision path** — render text-layerless/figure-dense PDF pages to PNG via `page.get_pixmap()`, persist screenshots to the `assets` table, build the missing `bad assets path/list` subcommand, add a figure/scanned-page reading instruction to width-sweep + ultrafast/fast fetcher contracts (both `src/.../skills/*.md` AND `.claude/skills/*/SKILL.md`) | multimodal | yes | ships-now | med-high | **+0.4** | **Real-mover (0→working).** Only axis scoring a literal zero; host already can see. Honest-discounted because it ships unmeasured unless paired with a figure-bearing eval case. |
| 4 | **Activate the dead recency apparatus** — one converter populating `published_days_ago`/`age_days` from `year`+`extract_published_date` at `Candidate` build/ingest; then CALL the existing `run_prefilter`/`passes_recency_gate` (currently zero callers) | freshness | yes | ships-now | low | **+0.3** | **Real-mover per Δ/effort.** Low effort, activates a tested-but-inert subsystem an auditor can verify changes ranking. |
| 5 | **Recency-biased query planning** — honor `recency_days` into provider calls (web `after:YYYY-MM-DD`, OpenAlex/Crossref/arXiv native date filters); set it from decompose's time_horizons | freshness | yes | ships-now | med | **+0.3** | **Real-mover (pairs with #4).** Closes freshness on the active-search side with free date filters. |
| 6 | **Vision-grounding rung** — store figure transcription as `quoted_support` + asset-anchor so figure-derived numbers stay inside the uncited/recitation gates | multimodal/grounding | yes | ships-now | med | **+0.15** | **Conditional must-do.** Only matters if #3 ships; keeps multimodal from becoming an ungrounded escape hatch (protects the moat). |
| 7 | **`bad export` to HTML/PDF with resolved citations** — wire the unused `serve/renderer.render_markdown` + fill the empty `export/` stub; resolve dangling `[N]`/`[[note-id]]` to a real references appendix | adoption | yes | ships-now | med | **+0.2** | **Real adoption win.** Today the shipped report has bare unresolved `[N]` brackets; makes the auditable-vault moat a shareable artifact. |
| 8 | **Grounding-recall harness** — mutation-driven (`grounding/recall_harness.py` + fixtures), `bad grounding-recall`, regression floor; honestly self-labeled | proof | yes | ships-now | high | **+0.2** | **Marginal, keep as regression-hardening.** Skeptic-confirmed circular on the hard band (FakeLLM scripts the verdict); honest value is locking deterministic-guard catch-rates + an honest "0% paraphrase-contradiction caught keyless" disclosure. Not the proof lever it claims. |
| 9 | **Per-claim grounding-audit appendix** — `bad grounding-surface` renders the already-computed `verify_score`/`confidence_band`/deciding-tier as an opt-in sidecar | proof | yes | ships-now | low | **+0.15** | **Marginal-good.** Cheap; makes the auditability moat clickable. No hosted DR exposes a per-claim confidence ledger. |
| 10 | **Scope HOW_IT_WORKS.md** off "no-hallucination/every sentence checked" to keyless reality; once #8 exists, replace with the measured catch-rate | proof | yes | ships-now | low | **+0.1** | **Cheap honesty fix.** Removes a linked-doc over-claim the auditor explicitly tracks. |
| — | `[local]` cross-encoder recall delta (keyless-vs-`[local]` on hard band) | proof | **no (GPU)** | **needs-user** | med | **+0.15** | **Needs-user.** Quantifies the optional neural lane; nice-to-have, not load-bearing. |

**Dropped as already-done (PR#8/#9, post-reaudit — verified in current code):** `_verify_report` standalone crash (now has try/except `VaultError` + `init_schema()`); 12.5-grader `passed:null` branch (Step 12.5.2b, "You ARE the host judge"); `needs_host_judgment` consumer (11.5/fast/ultrafast skills now say "re-judge it yourself"). **Dropped as marginal/noise:** cross-run vault recall (skeptic-confirmed +0.1 — notes already persist, dedup seed-set mis-described, attacks the wrong "scale"); `bad status`/`resume`/`try`/`extend` (adoption polish, real but ~+0.1 each and don't lift the ceiling); contradiction→CRITICAL writeback as a standalone (+0.2 but the keyless detector mostly can't *see* the contradictions it would block — fold into #1's host-judge path, not a separate lever).

## 3. Minimal set to credibly reach 9

**The load-bearing core (without these, 9 is impossible): #1 + #2.** The score is capped at 7 *primarily* on proof, and both auditors name the real head-to-head as the sole pitch-converter. #2 cannot run honestly keyless without #1 (the only semantic judge needs a paid key; the keyless RubricJudge auto-fails a corpus-less competitor and an auditor discounts it to near-zero). So **#1 is the precondition and #2 is the payoff** — together a verified **~+0.9** (honest, not the naive +0.9 each lens claimed; a single-competitor, host-self-judged, N=8–12 blinded result is a *real but bounded* data point an honest auditor credits as "demonstrated on its axes," not a clean sweep).

**Then add genuine new capability over the Claude+WebSearch baseline: #3 + #4 + #5 (+#6 to keep #3 honest).** This is the other half of the mandate. Vision (#3+#6, ~+0.5) takes the one literal-zero axis to working-and-grounded; recency activation+planning (#4+#5, ~+0.6) makes a tested subsystem actually fire on both the scoring and search sides. These are the levers that make the proof in #2 *true* rather than lucky — a freshness-gated, figure-reading run is what wins (or honestly loses) the head-to-head.

**Honest arithmetic (deltas do NOT sum naively — #1/#2/#8/#9/#10 all draw credibility from the same proof axis):**
- Proof axis (#1+#2+#9+#10, with #8 as regression floor): realistic **+0.8 to +1.0** combined (capped — it's one axis).
- Capability axis (#3+#4+#5+#6): **+0.7 to +0.9** of genuinely new value over baseline.
- Adoption (#7): +0.2 standalone.

Net realistic ceiling with the full minimal set landed and #2 run: **~8.7–9.0.** The last ~0.3 is **entirely on #2 actually being executed by the user** — code alone caps at ~8.5–8.7.

**The two items 9 cannot exist without: #1 (host-judge seam) and #2 (the real run).** Everything else raises real value but is creditable as "improved-but-still-7-shaped" until proof lands.

## 4. Ship-now build order (keyless, no user)

**First — unblock proof + activate the cheapest real capability (1–2 days):**
1. **HostJudge seam (#1).** New `Judge` impl (mirror `LLMJudge` but JSON-write-then-read at the `provider.complete` boundary; the `Judge` Protocol at `judge.py:102` + `AxisRails.from_raw`/`from_rails` make this a true drop-in). Add `bad headtohead --emit-judge-tasks` (writes blinded prompts) and `--verdicts <dir>` (ingests host-written rails). **Blind BEFORE writing and strip entrant names to opaque ids** (the skeptic's integrity caveat — `blind_report`/`TOOL_MARKERS` already exist; reuse them) or the result reads as self-graded. Add a thin `bad-research-headtohead` skill ("You ARE the host model — grade each blinded pair").
2. **Recency activation (#4).** One converter at `Candidate` build (`dedup.py:55`) / funnel ingest: `published_days_ago = (today − pubdate).days` from `metadata['year']` + `fetch_clean.extract_published_date`; write `meta['age_days']` (read at `rank.py:77`) and `Candidate.published_days_ago` (read at `prefilter.py:257`). Then **wire the orphaned `run_prefilter`/`passes_recency_gate` into the funnel orchestrator** (currently zero callers — this is the real bug, not just the missing converter).

**Second — the multimodal 0→1 and freshness search side (3–5 days):**
3. **Vision path (#3+#6).** In `fetch_clean.pdf_to_markdown`, on empty/figure-dense, `page.get_pixmap()`→PNG (pymupdf already imported); **build the missing assets-write path (`INSERT INTO assets`) and the `bad assets path/list` subcommand** (documented but absent — fixes the `agent_docs.py:84` vaporware too); persist `crawl4ai` `screenshot_bytes` (captured at `:270`, never written). Add the figure-reading instruction to `bad-research-2-width-sweep.md` + fast/ultrafast fetcher contracts (**edit BOTH `src/.../skills/*.md` and `.claude/skills/*/SKILL.md` — dual-sourced, must survive the Codex transform**). Store figure transcription as `quoted_support` so it rides the existing gates (#6).
4. **Recency-biased planning (#5).** Honor `recency_days` into provider calls (web date operators + OpenAlex `from_publication_date`/Crossref `from-pub-date`/arXiv date range); set from decompose time_horizons.

**Third — proof-surface + adoption (2–3 days):**
5. **`bad export` (#7)** (wire `serve/renderer` + resolve `[N]`), **`bad grounding-surface` (#9)**, **grounding-recall harness (#8)** with honest per-mutation labels, **scope HOW_IT_WORKS.md (#10)**.

## 5. What needs the user

- **The real head-to-head (#2) — the load-bearing one.** The tool by design never calls Gemini/OpenAI/Perplexity/Grok (`headtohead.py:6`). The user must: run 8–12 `starter_set.json` queries through ONE commercial DR tool, paste reports to `runs/competitor/`, run ultrafast on the same strings, hand-build the competitor corpus JSON (per RUNBOOK §3, so `source_quality` is scored fairly), then `bad headtohead --emit-judge-tasks`/`--verdicts` (keyless via #1) and commit the blinded scorecard. **This unlocks the entire proof axis** — it is what an honest re-auditor needs to move "beats commercial DR" from UNDEMONSTRATED to demonstrated-on-its-axes. Without it the ceiling is ~8.5.
- **`[local]` cross-encoder recall delta.** A machine with `sentence-transformers`/torch to run the recall harness in `[local]` mode and publish the keyless-vs-neural catch-rate delta on the hard paraphrase band. Unlocks: proof that the architecture degrades gracefully + quantifies the neural lane. Nice-to-have, ~+0.15.
- **(Optional) Codex on-device verification.** Confirms the dual-sourced skill edits (#3) survive the Codex transform. Not score-bearing on the Claude path, but the audit flagged Codex render leaks/invalid YAML — worth the user's device-check before any Codex ship.

## 6. Honest ceiling

**9/10 is reachable for a keyless host-model skill — but only if #2 is actually run, and there is a hard cap below 9 on one axis we should stop pretending about: live real-time freshness/breadth.** The recency levers (#4/#5) are genuine and worth doing — they activate a dead subsystem and add date-filtered scholarly + dated-web pulls no Claude+WebSearch baseline does — but they **cannot close the live-crawl gap**: a keyless host-model tool has no warm index, no anti-bot extraction at scale, no neural retrieval over a fresh crawl. The honest framing is "freshness-*aware and freshness-*honest* (states evidence age, prefers fresh)," not "freshness-*competitive*." Likewise multimodal (#3) goes 0→working on figures/scanned PDFs the host can see, but it will not match a hosted tool's systematic vision over hundreds of pages. **So the path to 9 is NOT to win every axis — it is to (a) PROVE the win on the slice it genuinely owns (auditable keyless grounding, deterministic ship-gate, controllability), via #1+#2, and (b) raise the two zero/dead axes to real-and-honest, via #3+#4+#5+#6.** A re-auditor scoring 9 will be crediting "proven on its axes + two real new capabilities + clean honesty surface," explicitly noting it still trades raw freshness/scale/speed to commercial DR. If #2 is never run, the honest ceiling is **~8.3–8.5** no matter how much else ships — proof is the one thing code here cannot manufacture.