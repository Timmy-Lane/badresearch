# Head-to-Head Runbook — how to actually run it

The `bad headtohead` apparatus exists and is tested, but a **win is only real once you run it**.
This is the turnkey procedure. The one part the tool cannot do for you is run the *competitor*
(it never calls Gemini/OpenAI/Perplexity/Grok) — you paste those reports in.

A complete worked example (two sample reports + the scorecard they produce) lives in
[`examples/`](examples/) — generated by the single-query command in Step 4a.

## 0. Decide scope

- **Query set:** the shipped [`queries/starter_set.json`](queries/starter_set.json) — 12 queries
  across all 8 modalities (causal, comparison, multi-domain, contested, definitional, recency,
  breadth-list, numeric). Use all 12, or a subset by editing a copy.
- **Competitor:** pick ONE commercial DR product to start (e.g. Gemini Deep Research). One fair,
  blinded comparison beats four sloppy ones.
- **Tier:** run bad-research's `ultrafast` (closest to the commercial "Deep Research" button).

## 1. Produce bad-research's reports (you, in a research workdir)

For each query, in your research working dir (e.g. `~/Desktop/researchfms`), run the skill in
Claude Code with the `ultrafast` route and save the final report:

```
/bad-research ultrafast mode: <paste the query text>
# → research/notes/final_report_<tag>.md  → copy to runs/bad/<query_id>.md
```

Record wall-clock minutes and (if you track it) cost per run — they become scorecard columns.

## 2. Produce the competitor's reports (you, manually)

Run the SAME query string through the chosen DR product; save each output verbatim to
`runs/competitor/<query_id>.md`. Don't edit them. (Tool-identifying text is fine — the harness
**blinds** both sides before judging, stripping markers like "Gemini"/"Perplexity"/"bad-research".)

## 3. (Optional but recommended) give each side a corpus

The `source_quality` axis scores a report against the evidence it had. Single-query/manifest runs
default to an empty corpus, so that axis reads 0 for both (see the example scorecard). To score it
fairly, add a `corpus_file` per entrant: a JSON list of `{note_id, url, text}` (bad-research's vault
notes export cleanly; for the competitor, paste the sources it cited). Otherwise just read the
other four axes.

## 4. Run the scorecard

**4a — quick start (one query, what the example uses):**
```
bad headtohead \
  --bad-report runs/bad/h2h_01_causal.md \
  --competitor-report runs/competitor/h2h_01_causal.md \
  --query-id h2h_01_causal --competitor-name gemini-deep-research \
  --out results/
```

**4b — full set (a manifest):** create `manifest.json`:
```json
{
  "bad_name": "bad-research-ultrafast",
  "competitor_name": "gemini-deep-research",
  "entrants": {
    "h2h_01_causal": [
      {"name": "bad-research-ultrafast", "report_file": "runs/bad/h2h_01_causal.md", "cost_usd": 0.0, "latency_s": 540},
      {"name": "gemini-deep-research",   "report_file": "runs/competitor/h2h_01_causal.md", "cost_usd": 0.20, "latency_s": 300}
    ]
  }
}
```
then:
```
bad headtohead -m manifest.json --out results/        # keyless RubricJudge (proxy)
bad headtohead -m manifest.json --out results/ --llm  # host-model judge (semantic; needs the host model)
```

## 5. Read the scorecard

`results/headtohead-scorecard.md` (+ `.json`) gives the per-query winner, per-axis mean rail-credit,
cost/latency, and the aggregate W-T-L from bad-research's point of view. Ties are never rounded to a
win; a missing report is counted a tie, never a silent win.

## Honest caveats (read before quoting a number)

- **The default keyless `RubricJudge` is a proxy** — citation-presence + word-overlap, NOT semantic
  entailment. It will pass a well-cited report that contradicts its sources. For a quotable result,
  run `--llm` (host-model judge) so content quality is actually assessed.
- **Competitor runs are manual**, so the comparison is only as fair as your runs (same query string,
  same day, no cherry-picking).
- **This measures the apparatus; the tally is whatever the real runs produced.** No win is claimed
  until you publish a scorecard. See [`HEAD_TO_HEAD.md`](HEAD_TO_HEAD.md) for the full protocol +
  blinding/scoring rationale.
